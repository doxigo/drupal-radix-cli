{#
/**
 * @file
 * Template for the Close button component.
 *
 * Bootstrap Documentation
 * https://getbootstrap.com/docs/5.3/components/close-button/
 *
 * Full List Utility Classes
 * https://github.com/twbs/bootstrap/blob/v5.3.0/dist/css/bootstrap.css#L5360
 *
 * Available config:
 * - size: btn-sm | btn-lg
 * - disabled: true | false
 * - attributes: Attributes array.
 * - utility_classes: An array of utility classes.
 */
#}


{% set size = size ? [size] : [] %}
{% set disabled = disabled ? ['disabled'] : [] %}
{% set utility_classes = utility_classes ? utility_classes : [] %}

{% set classes = [
  'btn',
  'btn-close',
]|merge(utility_classes)|merge(size)|merge(disabled) %}

<button type="button" {{ attributes.addClass(classes) }} {{ disabled }} aria-label="{{ 'Close'|trans }}"></button>